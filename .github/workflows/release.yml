name: Create Release
on:
  push:
    tags:
      - 'agent-v*'

env:
  BINARY_NAME: smartdns-log-agent

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Wait for builds to complete dynamically
        run: |
          echo "Dynamically waiting for builds to complete..."
          
          # 获取当前 commit SHA
          COMMIT_SHA="${{ github.sha }}"
          REPO="${{ github.repository }}"
          
          # 需要等待的 workflows
          WORKFLOWS=("Build Binaries" "Create Release Packages")
          
          # 等待函数
          wait_for_workflow() {
            local workflow_name="$1"
            local max_wait=1200  # 最大等待20分钟
            local check_interval=30  # 每30秒检查一次
            local waited=0
          
            echo "⏳ Waiting for '$workflow_name' to complete..."
          
            while [ $waited -lt $max_wait ]; do
              # 获取该 workflow 在当前 commit 的运行状态
              runs=$(gh api "repos/$REPO/actions/runs" \
                --jq ".workflow_runs[] | select(.head_sha == \"$COMMIT_SHA\" and .name == \"$workflow_name\") | .status" \
                2>/dev/null || echo "")
          
              if echo "$runs" | grep -q "completed"; then
                # 检查是否成功
                conclusion=$(gh api "repos/$REPO/actions/runs" \
                  --jq ".workflow_runs[] | select(.head_sha == \"$COMMIT_SHA\" and .name == \"$workflow_name\") | .conclusion" \
                  2>/dev/null || echo "")
          
                if echo "$conclusion" | grep -q "success"; then
                  echo "✅ '$workflow_name' completed successfully!"
                  return 0
                else
                  echo "❌ '$workflow_name' completed but failed. Conclusion: $conclusion"
                  return 1
                fi
              elif echo "$runs" | grep -q "in_progress"; then
                echo "🔄 '$workflow_name' is still running... (waited ${waited}s)"
              else
                echo "🔍 '$workflow_name' not found or not started yet... (waited ${waited}s)"
              fi
          
              sleep $check_interval
              waited=$((waited + check_interval))
            done
          
            echo "⏰ Timeout waiting for '$workflow_name' (waited ${waited}s)"
            return 1
          }
          
          # 等待所有必要的 workflows
          all_success=true
          for workflow in "${WORKFLOWS[@]}"; do
            if ! wait_for_workflow "$workflow"; then
              echo "⚠️  Warning: '$workflow' did not complete successfully"
              all_success=false
            fi
          done
          
          if [ "$all_success" = true ]; then
            echo "🎉 All workflows completed successfully!"
          else
            echo "⚠️  Some workflows failed, but continuing with release creation..."
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Download artifacts with smart retry
        run: |
          echo "📦 Downloading artifacts..."
          
          download_artifact() {
            local artifact_name="$1"
            local max_attempts=5
            local attempt=1
          
            while [ $attempt -le $max_attempts ]; do
              echo "Attempt $attempt/$max_attempts: Downloading $artifact_name..."
          
              if gh run download --name "$artifact_name" 2>/dev/null; then
                echo "✅ Successfully downloaded $artifact_name"
                return 0
              else
                echo "❌ Failed to download $artifact_name (attempt $attempt)"
                if [ $attempt -lt $max_attempts ]; then
                  sleep 10
                fi
              fi
          
              attempt=$((attempt + 1))
            done
          
            echo "⚠️  Failed to download $artifact_name after $max_attempts attempts"
            return 1
          }
          
          # 尝试下载所有包
          packages=("linux-amd64" "linux-arm64" "linux-armv7" "darwin-amd64" "darwin-arm64" "windows-amd64.exe")
          downloaded_count=0
          
          for suffix in "${packages[@]}"; do
            if download_artifact "package-$suffix"; then
              downloaded_count=$((downloaded_count + 1))
            fi
          done
          
          echo "📊 Downloaded $downloaded_count out of ${#packages[@]} packages"
          
          # 如果没有下载到任何包，尝试下载二进制文件
          if [ $downloaded_count -eq 0 ]; then
            echo "🔄 No packages found, trying to download binaries..."
            for suffix in "${packages[@]}"; do
              download_artifact "binary-$suffix"
            done
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Prepare release files
        run: |
          mkdir -p releases
          
          # 查找 tar.gz 包文件
          find . -name "*.tar.gz" -exec cp {} releases/ \; 2>/dev/null || true
          
          # 如果没有包文件，但有二进制文件，快速创建包
          if [ $(ls releases/*.tar.gz 2>/dev/null | wc -l) -eq 0 ]; then
            echo "📦 No packages found, creating packages from binaries..."
          
            for binary in ${{ env.BINARY_NAME }}-*; do
              if [ -f "$binary" ]; then
                suffix=${binary#${{ env.BINARY_NAME }}-}
                echo "Creating package for $suffix..."
          
                mkdir -p "temp-$suffix"
                cp "$binary" "temp-$suffix/"
          
                # 创建简单的安装脚本
                cat > "temp-$suffix/install.sh" << 'EOF'
          #!/bin/bash
          echo "Installing SmartDNS Log Agent..."
          sudo cp smartdns-log-agent-* /usr/local/bin/smartdns-log-agent 2>/dev/null || sudo cp smartdns-log-agent /usr/local/bin/
          sudo chmod +x /usr/local/bin/smartdns-log-agent
          echo "Installation completed!"
          EOF
                chmod +x "temp-$suffix/install.sh"
          
                tar -czf "releases/${{ env.BINARY_NAME }}-$suffix.tar.gz" -C "temp-$suffix" .
                rm -rf "temp-$suffix"
              fi
            done
          fi
          
          # 统计结果
          file_count=$(ls releases/*.tar.gz 2>/dev/null | wc -l)
          echo "📊 Final release files count: $file_count"
          
          if [ $file_count -gt 0 ]; then
            echo "📋 Release files:"
            ls -la releases/
          else
            echo "⚠️  No release files created, adding placeholder"
            touch releases/README.txt
            echo "Release packages are being built. Please check the Actions tab." > releases/README.txt
          fi

      - name: Generate dynamic release notes
        run: |
          file_count=$(ls releases/*.tar.gz 2>/dev/null | wc -l)
          
          cat > release-notes.md << EOF
          ## SmartDNS Log Agent ${{ github.ref_name }}
          
          高性能的 SmartDNS 日志收集和分析代理。
          
          $(if [ $file_count -gt 0 ]; then
            echo "### 🚀 快速开始"
            echo ""
            echo "\`\`\`bash"
            echo "# 下载对应平台的包"
            echo "wget https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/smartdns-log-agent-linux-amd64.tar.gz"
            echo "tar -xzf smartdns-log-agent-linux-amd64.tar.gz"
            echo ""
            echo "# 安装"
            echo "sudo ./manage.sh install 2>/dev/null || sudo ./install.sh"
            echo "sudo ./manage.sh start 2>/dev/null || echo 'Manual start required'"
            echo "\`\`\`"
            echo ""
            echo "### 📦 可用平台 ($file_count 个包)"
          else
            echo "### ⏳ 发布包构建中..."
            echo ""
            echo "二进制文件和安装包正在后台构建，请："
            echo "1. 稍后刷新此页面查看完整包"
            echo "2. 或从 [Actions](https://github.com/${{ github.repository }}/actions) 页面直接下载"
            echo ""
            echo "### 📦 即将支持的平台"
          fi)
          
          - Linux (amd64, arm64, armv7)
          - macOS (amd64, arm64) 
          - Windows (amd64)
          ---
          *构建时间: $(date -u '+%Y-%m-%d %H:%M:%S UTC')*
          EOF

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: SmartDNS Log Agent ${{ github.ref_name }}
          body_path: release-notes.md
          files: releases/*
          draft: false
          prerelease: ${{ contains(github.ref_name, 'rc') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') }}
          generate_release_notes: true
          make_latest: true